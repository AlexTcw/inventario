package com.inventario.core.config;

//@Configuration
//@EnableWebSecurity
public class SecurityConfig {
	/*
	 * @Bean public InMemoryUserDetailsManager userDetailsService() { UserDetails
	 * user1 = User.withUsername("user1")
	 * .password(passwordEncoder().encode("user1Pass")) .roles("USER") .build();
	 * UserDetails user2 = User.withUsername("user2")
	 * .password(passwordEncoder().encode("user2Pass")) .roles("USER") .build();
	 * UserDetails admin = User.withUsername("admin")
	 * .password(passwordEncoder().encode("adminPass")) .roles("ADMIN") .build();
	 * return new InMemoryUserDetailsManager(user1, user2, admin); }
	 * 
	 * @Bean public PasswordEncoder passwordEncoder() { return new
	 * BCryptPasswordEncoder(); }
	 */
}
